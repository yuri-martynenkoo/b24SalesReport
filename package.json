{
  "name": "b24-sales-report",
  "version": "1.0.0",
  "private": true,
  "description": "Embedded Bitrix24 sales analytics dashboard for VibeCode Black Hole",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

